import {SystemBase} from './SystemBase.ts';

/**
 * Tracks the player's distance-based score during a run.
 * Currently, a stub awaiting full implementation.
 *
 * @example
 * ```ts
 * gameSystems.register('score', new ScoreSystem());
 * ```
 */
export class ScoreSystem extends SystemBase {}
